import { Volume } from "./volume";

export class DatatableBuilder {

    /**
     * 
     */
    // buildByVolumes(volumes: Volume[]){
    //     let dataTable = volumes.map(volume => {
    //         return columns: []
    //     });
    // }
}